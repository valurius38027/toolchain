name: GitHub Release upload probe

on:
  push:
    paths:
      - ".github/workflows/release-upload-probe.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release-probe:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Create deterministic probe asset
        shell: bash
        run: |
          set -e